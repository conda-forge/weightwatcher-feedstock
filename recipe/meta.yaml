{% set name = "weightwatcher" %}
{% set version = "0.55" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/calculatedcontent/weightwatcher/archive/{{ version }}.tar.gz
  sha256: 2fe0017457dfd250d343107fb45093c60396944a6d421e579a7f8d8b49b84a91

build:
  number: 0
  entry_points:
    - weightwatcher = weightwatcher:main
  noarch: python
  script: |
    echo "DEFAULT_PEFT = False" >> weightwatcher/constants.py  # [not win]
    echo "from .weightwatcher import main" >> weightwatcher/__init__.py  # [not win]
    echo DEFAULT_PEFT = False >> weightwatcher/constants.py  # [win]
    echo from .weightwatcher import main >> weightwatcher/__init__.py  # [win]
    {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - numpy
    - pandas
    - matplotlib-base
    - matplotlib-inline
    - powerlaw
    - scikit-learn
    - safetensors
    - tqdm

test:
  imports:
    - weightwatcher
  commands:
    - pip check
    - weightwatcher --help
  requires:
    - python {{ python_min }}
    - pip

about:
  summary: A tool for analyzing and visualizing the weight distribution of neural networks.
  home: https://github.com/calculatedcontent/weightwatcher
  license: Apache-2.0
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - sarthakpati
